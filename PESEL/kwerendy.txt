zapytanie 1:
CREATE USER 'Agnieszka'@'localhost' 
IDENTIFIED BY 'haslo4';

zapytanie 2:
SELECT AVG(rocznik) 
FROM samochody 
WHERE marka = 'opel';

zapytanie 3:
SELECT marka, klienci.imie 
FROM samochody 
JOIN wypozyczenia ON samochody.id = wypozyczenia.samochody_id 
JOIN klienci ON wypozyczenia.klienci_id = klienci.id;

zapytanie 4:
