kwerenda 1:
SELECT * 
FROM pogoda 
WHERE miasta_id = 1 
ORDER BY data_prognozy ASC;

kwerenda 2:
SELECT id, data_prognozy 
FROM pogoda 
WHERE opady > 30;

kwerenda 3:
SELECT data_prognozy, temperatura_noc, miasta.nazwa 
FROM pogoda 
JOIN miasta 
ON miasta.id = pogoda.miasta_id 
WHERE temperatura_noc < 6; 

kwerenda 4:
ALTER TABLE miasta 
ADD COLUMN wojewodztwo text;