zapytanie 1:
CREATE TABLE uzytkownicy (
    id integer unsigned AUTO_INCREMENT, 
    login varchar(50), 
    haslo varchar(50), PRIMARY KEY (id)
);

zapytanie 2:
INSERT INTO uzytkownicy VALUES (NULL, 'Grzegorz', 'g');

zapytanie 3:
SELECT login 
FROM uzytkownicy;

zapytanie 4:
SELECT id, login 
FROM uzytkownicy 
WHERE haslo LIKE '4%';